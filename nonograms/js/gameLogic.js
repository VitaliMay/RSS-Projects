import { data } from './data.js';
import { CanvasGrid, canvasGame } from './canvasDraw.js';

// let canvasGame = new CanvasGrid(5, 40, 2, data.butterfly.matrix);
// let canvasGame = new CanvasGrid(5, 40, 2, data.butterfly.matrix);

// const canvasGame = {
//   currentGame: new CanvasGrid(5, 40, 2, data.butterfly.matrix),
// };

const hello = 'Привет game logic';
// console.log(hello);

export { hello };
